{
  "name": "exovia-neurocanvas",
  "version": "0.9.0",
  "private": true,
  "description": "Offline-first visual knowledge explorer and auditable human-AI secondary brain.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.mjs",
    "verify:static": "node scripts/verify-frontend.mjs",
    "test:e2e": "playwright test",
    "verify": "npm run validate && npm run verify:static && npm run test && npm run test:e2e",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
