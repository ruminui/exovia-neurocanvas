name: Validate ChatGPT App

on:
  push:
    branches: [main]
    paths:
      - 'chatgpt-app/**'
      - '.devcontainer/**'
      - 'JUDGE_START_HERE.md'
      - '.github/workflows/chatgpt-app.yml'
  pull_request:
    paths:
      - 'chatgpt-app/**'
      - '.devcontainer/**'
      - 'JUDGE_START_HERE.md'
      - '.github/workflows/chatgpt-app.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-chatgpt-app:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: chatgpt-app
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Syntax and unit tests
        run: npm run verify

      - name: Complete hackathon judge flow
        run: npm run judge

      - name: Upload judge evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: exovia-chatgpt-app-judge-evidence
          path: chatgpt-app/judge-output
          if-no-files-found: error
          retention-days: 30

      - name: Build hardened container
        run: docker build --tag exovia-prooflayer:ci .

      - name: Container runtime sanity
        shell: bash
        run: |
          set -euo pipefail
          docker run --detach --rm --name exovia-prooflayer-ci --publish 8798:8787 exovia-prooflayer:ci
          trap 'docker stop exovia-prooflayer-ci >/dev/null 2>&1 || true' EXIT
          for attempt in $(seq 1 30); do
            if curl --fail --silent http://127.0.0.1:8798/health; then
              exit 0
            fi
            sleep 1
          done
          docker logs exovia-prooflayer-ci
          exit 1
